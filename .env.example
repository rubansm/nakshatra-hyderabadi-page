# Google Apps Script Web App URL — receives orders & leads,
# sends the customer email, pings the owner on Telegram.
# Get this from: Apps Script → Deploy → New deployment → Web app
VITE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/REPLACE_ME/exec
