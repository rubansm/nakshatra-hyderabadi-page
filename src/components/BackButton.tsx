// Back navigation is now handled inside the Navbar on inner pages.
// This component is kept as a no-op to avoid touching every page that imports it.
const BackButton = () => null;

export default BackButton;
